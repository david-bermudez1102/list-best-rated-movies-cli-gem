RSpec.describe ListBestRatedMovies do
  it "has a version number" do
    expect(ListBestRatedMovies::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
